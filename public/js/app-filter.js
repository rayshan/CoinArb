// Generated by CoffeeScript 1.6.3
(function() {
  var filter;

  filter = angular.module('CaAppFilter', []);

  filter.filter('pct', function() {
    return function(input) {
      if (input !== 0) {
        if (Math.abs(input) < 1) {
          return "" + ((input * 1).toFixed(2)) + " %";
        } else if (Math.abs(input) < 10) {
          return "" + ((input * 1).toFixed(1)) + " %";
        } else {
          return "" + ((input * 1).toFixed(0)) + " %";
        }
      }
    };
  });

  filter.filter('round', function() {
    return function(input) {
      return (input * 1).toFixed(2);
    };
  });

  filter.filter('toArray', function() {
    return function(input) {
      var i, out;
      out = [];
      for (i in input) {
        out.push(input[i]);
      }
      return out;
    };
  });

}).call(this);

/*
//@ sourceMappingURL=app-filter.map
*/
